# Variables set here are not necessarily available as environment variables in your shell.
# To add any, modify the appropriate list in env-vars.sh

VPC_NAME: "{{ cookiecutter.vpc_name }}"

AWS_DEFAULT_REGION:  "{{ cookiecutter.aws_region }}"
AWS_AVAILABILITY_ZONES: "{{ cookiecutter.aws_availability_zones }}"
AWS_AVAILABILITY_ZONE_COUNT: "{{ cookiecutter.aws_availability_zone_count }}"

ANSIBLE_CONFIG: '$INFRASTRUCTURE_REPO/config/private/ansible.cfg'
KUBECONFIG: '$INFRASTRUCTURE_REPO/config/private/kube_config'
AWS_INVENTORY_PATH: '$INFRASTRUCTURE_REPO/plugins/'

INFRASTRUCTURE_BUCKET: "{{ cookiecutter.infrastructure_bucket }}"
